package com.cleany.analytics;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AcquisitionCampaignEntryRepository extends JpaRepository<AcquisitionCampaignEntry, Long> {
}
