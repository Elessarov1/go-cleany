# Mock APIs and the hardcoded preview identity are enabled only explicitly.
VITE_PREVIEW_MODE=true

# Keep empty for same-origin API access. A separate URL is useful only for
# exceptional local setups; the Vite dev server proxies backend paths by default.
VITE_API_BASE_URL=
